<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!-- NFC HCE service: ISO 7816-4 APDU handler for CylinderSeal payments.
             The AID matches the one generated by cs-mobile-core::wire::build_apdu_frames. -->
        <service
            android:name=".nfc.CylinderSealApduService"
            android:exported="true"
            android:permission="android.permission.BIND_NFC_SERVICE">
            <intent-filter>
                <action android:name="android.nfc.cardemulation.action.HOST_APDU_SERVICE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <meta-data
                android:name="android.nfc.cardemulation.host_apdu_service"
                android:resource="@xml/cylinder_seal_apduservice" />
        </service>
    </application>

</manifest>
